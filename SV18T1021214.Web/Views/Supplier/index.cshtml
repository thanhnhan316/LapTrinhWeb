@model SV18T1021214.Web.Models.PaginationSearchInput
@{
    ViewBag.Title = "Nhà cung cấp";
}

<!-- search form -->
<form id="searchInput" action="~/supplier/search">
    <div class="input-group">
        @Html.HiddenFor(m => m.PageSize)
        <input type="text" name="searchValue" class="form-control" placeholder="Search..." autofocus value="@Model.SearchValue">
        <span class="input-group-btn">
            <button type="submit" name="searchValue" id="search-btn" class="btn btn-flat btn-primary">
                <i class="fa fa-search"></i>
            </button>
            <a href="~/supplier/create" class="btn btn-flat btn-primary" style="margin-left:5px">
                Bổ sung nhà cung cấp
            </a>
        </span>
    </div>
</form>

<div id="searchResult">

</div>

@section scripts {
    <script src="~/Scripts/search.js"></script>
    <script>
        $(document).ready(function () {
            doSearch(@Model.Page);
            $("#searchInput").submit(function (e) {
                e.preventDefault();
                doSearch(1);
                return false;
            });
        } );
    </script>
}



